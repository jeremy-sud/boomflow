name: "🎯 BOOMFLOW Event Processor"

on:
  # === PULL REQUESTS ===
  pull_request:
    types: [opened, closed]
  
  # === CODE REVIEWS ===
  pull_request_review:
    types: [submitted]
  
  # === ISSUES ===
  issues:
    types: [opened, closed]
  
  # === RELEASES ===
  release:
    types: [published]
  
  # === Also run on push (for commit tracking) ===
  push:
    branches: [main]
  
  # === Manual trigger for testing ===
  workflow_dispatch:
    inputs:
      test_user:
        description: 'User to simulate event'
        required: false
        default: 'jeremy-sud'

permissions:
  contents: write

jobs:
  process-event:
    name: "🏅 Process Event → Badges"
    runs-on: ubuntu-latest
    
    # Don't run for bots
    if: github.actor != 'dependabot[bot]' && github.actor != 'github-actions[bot]'
    
    steps:
      - name: Checkout BOOMFLOW
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Determine event type
        id: event_type
        run: |
          EVENT_NAME="${{ github.event_name }}"
          ACTION="${{ github.event.action }}"
          
          echo "📝 Event: $EVENT_NAME"
          echo "🔄 Action: $ACTION"
          
          # Build the full event type
          if [ "$EVENT_NAME" == "pull_request" ]; then
            if [ "$ACTION" == "closed" ] && [ "${{ github.event.pull_request.merged }}" == "true" ]; then
              EVENT_TYPE="pull_request.merged"
            else
              EVENT_TYPE="pull_request.$ACTION"
            fi
          elif [ "$EVENT_NAME" == "pull_request_review" ]; then
            EVENT_TYPE="pull_request_review.$ACTION"
          elif [ "$EVENT_NAME" == "issues" ]; then
            EVENT_TYPE="issues.$ACTION"
          elif [ "$EVENT_NAME" == "release" ]; then
            EVENT_TYPE="release.$ACTION"
          elif [ "$EVENT_NAME" == "push" ]; then
            EVENT_TYPE="push"
          else
            EVENT_TYPE="$EVENT_NAME"
          fi
          
          echo "event_type=$EVENT_TYPE" >> $GITHUB_OUTPUT
          echo "✅ Event type: $EVENT_TYPE"
      
      - name: Show event information
        env:
          EVENT_TYPE: ${{ steps.event_type.outputs.event_type }}
          ACTOR: ${{ github.actor }}
          REPO: ${{ github.repository }}
          REF: ${{ github.ref }}
          EVENT_NAME: ${{ github.event_name }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_AUTHOR: ${{ github.event.pull_request.user.login }}
          PR_MERGED: ${{ github.event.pull_request.merged }}
          REVIEW_USER: ${{ github.event.review.user.login }}
          REVIEW_STATE: ${{ github.event.review.state }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
        run: |
          echo "═══════════════════════════════════════════════════════"
          echo "📋 EVENT INFORMATION"
          echo "═══════════════════════════════════════════════════════"
          echo "🎯 Event: $EVENT_TYPE"
          echo "👤 Actor: $ACTOR"
          echo "📦 Repo: $REPO"
          echo "🔀 Ref: $REF"
          
          # Specific info per event type
          if [ "$EVENT_NAME" == "pull_request" ]; then
            echo "📝 PR: #$PR_NUMBER"
            echo "📌 Title: $PR_TITLE"
            echo "👤 Author: $PR_AUTHOR"
            echo "🔀 Merged: $PR_MERGED"
          elif [ "$EVENT_NAME" == "pull_request_review" ]; then
            echo "📝 PR: #$PR_NUMBER"
            echo "👤 Reviewer: $REVIEW_USER"
            echo "📊 Status: $REVIEW_STATE"
          elif [ "$EVENT_NAME" == "issues" ]; then
            echo "📝 Issue: #$ISSUE_NUMBER"
            echo "📌 Title: $ISSUE_TITLE"
          fi
          echo "═══════════════════════════════════════════════════════"
      
      - name: Process event and award badges
        id: process
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "🏅 Processing event..."
          node scripts/process-event.js "${{ steps.event_type.outputs.event_type }}"
      
      - name: Check for changes
        id: changes
        run: |
          if [[ -n $(git status --porcelain users/) ]]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
            echo "📝 Changes detected in user files:"
            git diff --stat users/
          else
            echo "has_changes=false" >> $GITHUB_OUTPUT
            echo "ℹ️ No changes to commit"
          fi
      
      - name: Commit and Push badges
        if: steps.changes.outputs.has_changes == 'true'
        run: |
          git config user.name "BOOMFLOW Bot"
          git config user.email "boomflow@ursol.com"
          
          # Event info for the commit
          EVENT_TYPE="${{ steps.event_type.outputs.event_type }}"
          ACTOR="${{ github.actor }}"
          
          git add users/
          git commit -m "🏅 Webhook: Badges awarded by $EVENT_TYPE
          
          Actor: @$ACTOR
          Event: $EVENT_TYPE
          Workflow Run: ${{ github.run_id }}
          
          🤖 Automatically awarded by BOOMFLOW Event Processor"
          
          git push
          
          echo "✅ Badges pushed successfully"
      
      - name: Summary
        run: |
          echo "═══════════════════════════════════════════════════════"
          echo "📊 SUMMARY"
          echo "═══════════════════════════════════════════════════════"
          if [ "${{ steps.changes.outputs.has_changes }}" == "true" ]; then
            echo "✅ Badges awarded and synced"
          else
            echo "ℹ️ No new badges for this event"
          fi
          echo "═══════════════════════════════════════════════════════"
